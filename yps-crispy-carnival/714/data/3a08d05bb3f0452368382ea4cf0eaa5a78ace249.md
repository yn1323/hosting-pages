# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: scenarios/multiActor/manager-role-removal.test.ts >> 管理者権限だけの解除 >> MM-P0-02: Bの管理画面権限だけを外し、A店のスタッフ所属を維持する
- Location: e2e/scenarios/multiActor/manager-role-removal.test.ts:12:3

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /\/dashboard\?shop=qd71cmb2pjenbrjbkwdkh8xz818b8fdh(?:&|$)/
Received string:  "http://localhost:3000/manager-invite?token=LiCa3JwX0c5iN7WopzLv97jvfm830wtGnTi0wx6nXHg"
Timeout: 20000ms

Call log:
  - Expect "toHaveURL" with timeout 20000ms
    43 × unexpected value "http://localhost:3000/manager-invite?token=LiCa3JwX0c5iN7WopzLv97jvfm830wtGnTi0wx6nXHg"

```

```yaml
- banner:
  - link "シフトリのトップページへ":
    - /url: /
    - img "シフトリ"
    - paragraph: シフトリ
    - paragraph: LINEで使えるシフト管理
- main:
  - status "招待情報を確認中":
    - heading "招待情報を確認しています" [level=1]
    - paragraph: このまましばらくお待ちください。
- region "Notifications, top (alt+T)"
```

# Test source

```ts
  1  | import { expect, type Page } from "@playwright/test";
  2  | 
  3  | const INVITATION_TIMEOUT = 20_000;
  4  | 
  5  | export class ManagerInvitationPage {
  6  |   constructor(private page: Page) {}
  7  | 
  8  |   async goto(token: string) {
  9  |     await this.page.goto(`/manager-invite?token=${encodeURIComponent(token)}`);
  10 |   }
  11 | 
  12 |   async acceptAndExpectDashboard(token: string, shopId: string) {
  13 |     await this.goto(token);
> 14 |     await expect(this.page).toHaveURL(new RegExp(`/dashboard\\?shop=${escapeRegExp(shopId)}(?:&|$)`), {
     |                             ^ Error: expect(page).toHaveURL(expected) failed
  15 |       timeout: INVITATION_TIMEOUT,
  16 |     });
  17 |   }
  18 | 
  19 |   async expectEmailMismatch(token: string) {
  20 |     await this.goto(token);
  21 |     await expect(
  22 |       this.page.getByRole("heading", { name: "ログイン中のメールアドレスが招待先と一致しません" }),
  23 |     ).toBeVisible({ timeout: INVITATION_TIMEOUT });
  24 |     await expect(this.page.getByRole("button", { name: "別のアカウントでログイン" })).toBeVisible();
  25 |   }
  26 | 
  27 |   async expectUsed(token: string) {
  28 |     await this.goto(token);
  29 |     await expect(this.page.getByRole("heading", { name: "この招待への参加は完了しています" })).toBeVisible({
  30 |       timeout: INVITATION_TIMEOUT,
  31 |     });
  32 |   }
  33 | }
  34 | 
  35 | function escapeRegExp(value: string) {
  36 |   return value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  37 | }
  38 | 
```