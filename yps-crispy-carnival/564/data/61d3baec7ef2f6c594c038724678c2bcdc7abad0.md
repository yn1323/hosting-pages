# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: scenarios/legal-consent-flow.test.ts >> 法務同意フロー >> スタッフはマジックリンクの同意ページで同意できる
- Location: e2e/scenarios/legal-consent-flow.test.ts:30:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('heading', { name: '利用規約・プライバシーポリシーの確認' })
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for getByRole('heading', { name: '利用規約・プライバシーポリシーの確認' })

```

```yaml
- status:
  - img "シフトリ"
  - paragraph: Loading...
- region "Notifications, top (alt+T)"
```

# Test source

```ts
  1  | import { expect, type Page } from "@playwright/test";
  2  | 
  3  | export class StaffLegalConsentPage {
  4  |   constructor(private page: Page) {}
  5  | 
  6  |   async goto(token: string) {
  7  |     await this.page.goto(`/legal/staff/consent?token=${token}`);
  8  |   }
  9  | 
  10 |   async expectConsentFormVisible() {
> 11 |     await expect(this.page.getByRole("heading", { name: "利用規約・プライバシーポリシーの確認" })).toBeVisible();
     |                                                                                  ^ Error: expect(locator).toBeVisible() failed
  12 |     await expect(this.legalConsentCheckbox()).toBeVisible();
  13 |   }
  14 | 
  15 |   async accept() {
  16 |     await this.page.locator("[data-scope='checkbox'][data-part='control']").click();
  17 |     await this.page.getByRole("button", { name: "同意する" }).click();
  18 |   }
  19 | 
  20 |   async expectAcceptedVisible() {
  21 |     await expect(this.page.getByText("同意済みです")).toBeVisible();
  22 |   }
  23 | 
  24 |   private legalConsentCheckbox() {
  25 |     return this.page.getByRole("checkbox", { name: /利用規約.*プライバシーポリシー/ });
  26 |   }
  27 | }
  28 | 
```